import { ProtectedPlaceholder } from '../../components/protected-placeholder'

export default function DashboardPage() {
  return <ProtectedPlaceholder title="Dashboard" description="See your workspace at a glance and keep every conversation moving." />
}
